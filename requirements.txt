# Financial Dashboard Requirements
# Python 3.14+ Compatible (No PyArrow Required)

# Data manipulation
pandas>=2.3.0
openpyxl>=3.1.5
numpy>=2.3.0

# Statistical analysis
scipy>=1.16.0

# Visualization
plotly>=6.3.0
matplotlib>=3.10.0
seaborn>=0.13.0

# Web framework
streamlit>=1.40.0

# Streamlit core dependencies
altair>=5.5.0
blinker>=1.9.0
cachetools>=6.2.0
click>=8.3.0
gitpython>=3.1.0
protobuf>=6.33.0
pydeck>=0.9.0
requests>=2.32.0
rich>=14.2.0
tenacity>=10.0.0
toml>=0.10.2
typing-extensions>=4.12.0
watchdog>=6.0.0

# NOTE: PyArrow is intentionally NOT included
# This dashboard uses HTML table rendering (df.to_html()) 
# instead of st.dataframe() or st.table() to avoid PyArrow dependency
# This ensures compatibility with Python 3.14+ where PyArrow wheels are unavailable
